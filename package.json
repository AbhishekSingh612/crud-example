{
  "name": "crud-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx webpack && wrangler deploy dist/worker.js",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.0.5",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4",
    "wrangler": "^3.60.3"
  }
}
